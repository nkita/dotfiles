gui:
  theme:
    activeBorderColor:
      - green
      - bold
    inactiveBorderColor:
      - white

git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never

os:
  editCommand: 'nvim'
  editCommandTemplate: '{{editor}} {{filename}}'
